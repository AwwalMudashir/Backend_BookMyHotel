package com.project.Backend_BookMyHotel.dto;

// Curated classification badges a room can carry, distinct from the freeform `amenities` map —
// these are the specific sustainability/work-friendly signals the case study calls out as
// something customers want to search and filter by, not just see listed as a feature.
public enum RoomTag {
    ECO_FRIENDLY, WORK_FRIENDLY
}
